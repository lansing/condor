# Frigate Remote Detector Server Configuration
# Extended options are commented out with their default values shown.

server:
  # ZMQ REP socket endpoint. Use tcp://*:<port> for TCP or ipc:// for Unix socket.
  endpoint: "tcp://*:5555"
  # Directory where model files are stored and received from Frigate.
  models_dir: "./models"

inference:
  # Inference backend / execution provider.
  # Options:
  #   "tensorrt"  — TensorRT engine (.engine file); requires condor:tensorrt image
  #                 with --runtime nvidia.  See docs/requirements/TENSORRT.md.
  #   "cpu"       — ONNX Runtime, CPU execution provider.
  #   "openvino"  — ONNX Runtime, OpenVINO execution provider (Intel GPU/CPU).
  #                 Requires onnxruntime-openvino: run make install-openvino.
  provider: "tensorrt"
  #provider_options:
  #  device: 0
  #provider: "openvino"


post_process:
  # Minimum confidence score to include a detection in results.
  # confidence_threshold: 0.4
  # Maximum number of detections to return (protocol max is 20).
  # max_detections: 20

# logging:
#   level: "INFO"  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
